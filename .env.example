# Server Configuration
NODE_ENV=production
PORT=3005

# Database (Optional - uses LocalDB if not provided)
MONGODB_URI=mongodb://localhost:27017/quiz-app

# Security (Optional)
ALLOWED_ORIGINS=https://yourdomain.com
